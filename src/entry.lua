local Fluent, SaveManager, InterfaceManager, Mobile, Translator, Localization = require("init")

_G.Fluent = Fluent
_G.SaveManager = SaveManager
_G.InterfaceManager = InterfaceManager

return Fluent, SaveManager, InterfaceManager, Mobile, Translator, Localization
